'use strict';

/*
function calcAge(birthYear) {
    const age = 2037 - birthYear;

    function printAge() {
        const output = `${firstName} are ${age}, born in ${birthYear}`;
        console.log(output);

        if (birthYear >= 1981 && birthYear <= 1996) {
            var millenial = true;
            const str = `Oh, and yua a millennial, ${firstName}`;
            console.log(str);
        }
        console.log(millenial);
    }
    printAge();
    return age;
}

const firstName = 'Kekich';
calcAge(1991);*/

console.log(me);
console.log(job);
console.log(year);

var me = 'wew';
let job = 'teacher';
const year = 1991;